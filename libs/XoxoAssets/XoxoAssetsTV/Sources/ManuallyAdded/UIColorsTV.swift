import UIKit

public enum UIColorsTV {
    public enum Background {
        public static let primary = UIColor(named: "background_primary")
        public static let surface = UIColor(named: "background_surface")
        public static let settings = UIColor(named: "background_settings")
    }
}
